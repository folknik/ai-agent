version: '3'

services:
  telegram_app:
    container_name: telegram_ai_agent
    build:
      context: .
      dockerfile: Dockerfile
    command: python -u src/agent.py
    restart: always
    env_file:
      - variables.env